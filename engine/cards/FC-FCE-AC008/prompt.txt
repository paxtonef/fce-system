Tu es un AI coder. Tu as une seule tâche à implémenter.
Le projet FCE existe déjà — ne recrée pas la structure.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+
Le projet est déjà structuré. Lis shared/blueprint.md et shared/interfaces.py.

[TA TÂCHE — AC-008]
Critère : "Garantir qu'un domaine inconnu retourne une erreur claire"
Fichier cible : src/fce/registry.py
Symbole : BuiltinPackRegistry.lookup

Interface exacte :
```python
class BuiltinPackRegistry:
    def lookup(self, pack_id: str) -> dict:
        """
        Si pack_id non enregistré →
          raise ValueError(f"Unsupported domain: {pack_id}. Available: finance_v1, health_v1, career_v1")
        Ne jamais tenter de classification sur domaine inconnu.
        """
        pass

    def get(self, pack_id: str) -> dict:
        return self.lookup(pack_id)
```

Tu dois faire passer : T-008

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_008.py
registry.lookup('domaine_inexistant') → ValueError avec la valeur du domaine
engine.evaluate() ne tente pas de classification sur domaine inconnu

[RÈGLES]
- Ne modifie que src/fce/registry.py et son test

[QUAND C'EST FAIT]
1. Crée work_result.json :
{
  "card_id": "FC-FCE-AC008",
  "status": "PASS",
  "files_changed": ["src/fce/registry.py", "tests/unit/test_ac_008.py"],
  "test_results": {"T-008": "pass"},
  "confidence": 0.95,
  "notes": ""
}

2. Place-le ici : results/FC-FCE-AC008/work_result.json

3. Push :
   git add results/FC-FCE-AC008/work_result.json
   git commit -m "result: FC-FCE-AC008 PASS"
   git push
