Tu es un AI coder. Tu as une seule tâche à implémenter.
Le projet FCE existe déjà — ne recrée pas la structure.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+
Le projet est déjà structuré. Lis shared/blueprint.md et shared/interfaces.py.

[TA TÂCHE — AC-004]
Critère : "Garantir que le score de confiance est toujours un nombre entre 0 et 1"
Fichier cible : src/fce/state_classifier.py
Symbole : StateClassifier.compute_confidence

Interface exacte :
```python
class StateClassifier:
    def compute_confidence(self, raw_score: float) -> float:
        """
        Clamp raw_score dans [0.0, 1.0].
        raw_score < 0.0 → 0.0 | raw_score > 1.0 → 1.0
        """
        pass
```

Tu dois faire passer : T-004

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_004.py
Asserter que confidence_score est float dans [0.0, 1.0]
Tester : raw=-0.5 → 0.0 et raw=1.5 → 1.0

[RÈGLES]
- Ne modifie que src/fce/state_classifier.py et son test
- confidence_score toujours float dans [0.0, 1.0]
- Résultat déterministe

[QUAND C'EST FAIT]
1. Crée work_result.json :
{
  "card_id": "FC-FCE-AC004",
  "status": "PASS",
  "files_changed": ["src/fce/state_classifier.py", "tests/unit/test_ac_004.py"],
  "test_results": {"T-004": "pass"},
  "confidence": 0.95,
  "notes": ""
}

2. Place-le ici : results/FC-FCE-AC004/work_result.json

3. Push :
   git add results/FC-FCE-AC004/work_result.json
   git commit -m "result: FC-FCE-AC004 PASS"
   git push
