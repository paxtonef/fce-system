Tu es un AI coder. Tu as une seule tâche à implémenter.
Le projet FCE existe déjà — ne recrée pas la structure.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+
Le projet est déjà structuré. Lis shared/blueprint.md et shared/interfaces.py.

[TA TÂCHE — AC-005]
Critère : "Garantir que les trois packs intégrés se chargent sans erreur"
Fichier cible : src/fce/registry.py
Symbole : BuiltinPackRegistry.load

Interface exacte :
```python
class BuiltinPackRegistry:
    def load(self, pack_id: str) -> dict:
        """
        Charge packs/{pack_id}.yaml
        Accepte : finance_v1, health_v1, career_v1
        Raise ValueError si pack_id inconnu.
        """
        pass

    def get(self, pack_id: str) -> dict:
        return self.load(pack_id)
```

Tu dois faire passer : T-005

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_005.py
registry.load('finance_v1') → pas d'erreur
Répéter pour health_v1 et career_v1
Chaque pack contient : pack_id, domain, constraints, actions

[RÈGLES]
- Ne modifie que src/fce/registry.py et son test

[QUAND C'EST FAIT]
1. Crée work_result.json :
{
  "card_id": "FC-FCE-AC005",
  "status": "PASS",
  "files_changed": ["src/fce/registry.py", "tests/unit/test_ac_005.py"],
  "test_results": {"T-005": "pass"},
  "confidence": 0.95,
  "notes": ""
}

2. Place-le ici : results/FC-FCE-AC005/work_result.json

3. Push :
   git add results/FC-FCE-AC005/work_result.json
   git commit -m "result: FC-FCE-AC005 PASS"
   git push
