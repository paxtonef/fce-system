Tu es un AI coder. Tu as une seule tâche à implémenter.
Le projet FCE existe déjà — ne recrée pas la structure.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+
Le projet est déjà structuré. Lis shared/blueprint.md et shared/interfaces.py.

[TA TÂCHE — AC-007]
Critère : "Garantir qu'une entrée manquante retourne une erreur claire"
Fichier cible : src/fce/engine.py
Symbole : evaluate

Interface exacte :
```python
class Engine:
    def evaluate(self, pack_id: str, context: dict | None = None) -> dict:
        """
        Si pack_id est None ou vide →
          raise ValueError('Missing required input: pack_id')
        Sinon → pipeline complet.
        Ne jamais retourner de résultat partiel.
        """
        pass
```

Tu dois faire passer : T-007

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_007.py
engine.evaluate(pack_id=None) → ValueError avec 'pack_id' dans le message
engine.evaluate(pack_id='') → même comportement

[RÈGLES]
- Ne modifie que src/fce/engine.py et son test
- Aucune logique métier dans engine.py

[QUAND C'EST FAIT]
1. Crée work_result.json :
{
  "card_id": "FC-FCE-AC007",
  "status": "PASS",
  "files_changed": ["src/fce/engine.py", "tests/unit/test_ac_007.py"],
  "test_results": {"T-007": "pass"},
  "confidence": 0.95,
  "notes": ""
}

2. Place-le ici : results/FC-FCE-AC007/work_result.json

3. Push :
   git add results/FC-FCE-AC007/work_result.json
   git commit -m "result: FC-FCE-AC007 PASS"
   git push
