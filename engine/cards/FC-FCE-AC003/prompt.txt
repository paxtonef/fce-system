Tu es un AI coder. Tu as une seule tâche à implémenter.
Le projet FCE existe déjà — ne recrée pas la structure.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+
Le projet est déjà structuré. Lis shared/blueprint.md et shared/interfaces.py.

[TA TÂCHE — AC-003]
Critère : "Garantir que la sortie contient toujours les contraintes appliquées et le raisonnement"
Fichier cible : src/fce/output_assembler.py
Symbole : OutputAssembler.assert_completeness

Interface exacte :
```python
class OutputAssembler:
    def assert_completeness(self, output: dict) -> None:
        """
        Vérifie que output['applied_constraints'] et output['reasoning']
        sont non-None et non-vides.
        Lève ValueError avec message explicite sinon.
        """
        pass

    def assemble(self, pack_data: dict, constraints: list[dict], score: float) -> dict:
        """Appelle assert_completeness() avant de retourner."""
        pass
```

Tu dois faire passer : T-003

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_003.py
Lancer engine.evaluate() avec fixture valide
Asserter que applied_constraints est non-vide
Asserter que reasoning est non-vide

Fixture : tests/fixtures/valid_pack.yaml (déjà dans le projet)

[RÈGLES]
- Ne modifie que src/fce/output_assembler.py et son test
- applied_constraints et reasoning toujours remplis
- confidence_score toujours float dans [0.0, 1.0]
- Résultat déterministe

[QUAND C'EST FAIT]
1. Crée work_result.json :
{
  "card_id": "FC-FCE-AC003",
  "status": "PASS",
  "files_changed": ["src/fce/output_assembler.py", "tests/unit/test_ac_003.py"],
  "test_results": {"T-003": "pass"},
  "confidence": 0.95,
  "notes": ""
}

2. Place-le ici : results/FC-FCE-AC003/work_result.json

3. Push :
   git add results/FC-FCE-AC003/work_result.json
   git commit -m "result: FC-FCE-AC003 PASS"
   git push
