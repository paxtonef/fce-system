Tu es un AI coder. Tu as une seule tâche à implémenter.
Ce projet est GREENFIELD : tu dois créer toute la structure du projet.
Les autres coders travailleront sur ce projet après toi — crée une base solide.

[CONTEXTE PROJET]
Projet : Freedom Constraint Engine (FCE) — SPEC-FCE-001
Langage : Python 3.11+

Structure COMPLÈTE à créer :
  src/fce/__init__.py
  src/fce/engine.py
  src/fce/pack_validator.py
  src/fce/registry.py
  src/fce/state_classifier.py        ← TON FICHIER CIBLE
  src/fce/constraint_activator.py
  src/fce/output_assembler.py
  packs/finance_v1.yaml
  packs/health_v1.yaml
  packs/career_v1.yaml
  cli/__init__.py
  cli/fce_cli.py
  tests/__init__.py
  tests/unit/__init__.py
  tests/fixtures/valid_pack.yaml
  pyproject.toml

[INTERFACES DES DÉPENDANCES — Ne pas modifier ces signatures]
Lis shared/interfaces.py dans ce repo pour les signatures exactes.

[TA TÂCHE — AC-001]
Critère : "Garantir que le moteur produit toujours le même résultat pour les mêmes données"
Fichier cible : src/fce/state_classifier.py
Symbole : StateClassifier.score

Interface exacte :
```python
class StateClassifier:
    def score(self, pack_data: dict, context: dict | None = None) -> float:
        """
        Retourne un confidence_score dans [0.0, 1.0].
        DOIT être déterministe : mêmes pack_data + même context → même float.
        Ne jamais utiliser random, time, uuid ou toute source non-déterministe.
        """
        pass

    def compute_confidence(self, raw_score: float) -> float:
        """
        Clamp raw_score dans [0.0, 1.0].
        Si raw_score < 0.0 → retourner 0.0
        Si raw_score > 1.0 → retourner 1.0
        """
        pass
```

[TESTS À IMPLÉMENTER]
Fichier : tests/unit/test_ac_001.py
Test T-001 :
  Appeler engine.evaluate() deux fois avec les mêmes arguments
  Asserter que les deux résultats sont identiques (hors timestamp/request_id)

Fixture : tests/fixtures/valid_pack.yaml (voir shared/fixtures/valid_pack.yaml)

[MODULES STUB]
Pour que les tests passent, crée des stubs minimaux pour les autres modules :
- pack_validator.py → validate() retourne pack_data inchangé
- registry.py → get() charge packs/{pack_id}.yaml
- constraint_activator.py → activate() retourne []
- output_assembler.py → assemble() retourne dict avec tous les champs requis
- engine.py → evaluate() appelle le pipeline complet

[PACKS INTÉGRÉS]
Crée des YAML minimaux valides pour :
  packs/finance_v1.yaml
  packs/health_v1.yaml
  packs/career_v1.yaml
Chaque pack doit avoir : pack_id, domain, constraints (list), actions (list)

[RÈGLES]
- blocked_actions et allowed_actions ne partagent jamais d'élément
- applied_constraints et reasoning toujours remplis
- confidence_score toujours float dans [0.0, 1.0]
- Résultat déterministe
- Aucune logique métier dans engine.py
- Aucune variable globale

[QUAND C'EST FAIT]
```json
{
  "card_id": "FC-FCE-AC001",
  "status": "PASS",
  "files_changed": ["src/fce/state_classifier.py", "tests/unit/test_ac_001.py"],
  "test_results": {"T-001": "pass"},
  "confidence": 0.95,
  "notes": ""
}
```
